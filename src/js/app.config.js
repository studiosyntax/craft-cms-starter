/**
 * App Configuration
 *
 * Central place to toggle features per project.
 * Import and pass to App in site.js when a non-default setup is needed.
 */
export const config = {
  /** Show preloader animation on first visit. Set false to skip entirely. */
  usePreloader: true,

  /** Enable Taxi.js page transitions. Set false for plain navigation. */
  useTransitions: true,
};
